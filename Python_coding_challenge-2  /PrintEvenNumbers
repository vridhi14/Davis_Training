# Function to print even numbers from 1 to N
def print_even(n):
    for i in range(1, n + 1):
        if i % 2 == 0:   # check if number is even
            print(i, end=" ")
#Input
n = 10

#Output
print_even(n)
