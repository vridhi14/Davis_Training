# Function to Count frequency
def frequency(list):
    freq = {}
    
    for num in list:
        if num in freq:
            freq[num] += 1
        else:
            freq[num] = 1
    
    return freq

#Calling The Count Frequency Function 
print(frequency([1,2,2,3]))
